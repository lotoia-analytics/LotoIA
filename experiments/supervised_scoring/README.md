# Supervised Scoring Registry

Camada institucional da primeira implementacao oficial de `score_ml` incremental.

Esta estrutura registra apenas modelos supervisionados interpretaveis, auxiliares e
comparaveis ao benchmark estatistico. O ranking estatistico, o score hibrido principal e o
benchmark oficial permanecem como referencias primarias.
